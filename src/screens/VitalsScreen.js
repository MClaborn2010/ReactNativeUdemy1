import { StyleSheet, Text, View } from "react-native";
import React from "react";

const VitalsScreen = (props) => {
  return (
    <View>
      <Text>VitalsScreen</Text>
    </View>
  );
};

export default VitalsScreen;

const styles = StyleSheet.create({});
