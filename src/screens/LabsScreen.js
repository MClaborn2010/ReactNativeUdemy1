import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LabsScreen = (props) => {
  return (
    <View>
      <Text>LabsScreen</Text>
    </View>
  );
};

export default LabsScreen;

const styles = StyleSheet.create({});
